# Hadoop_despliegue
GoogleColab, donde se detatalla como desplegar un Hadoop dockerizado y realizar unas consultas al sistema de archivos HDFS
* Tenemos el archivo de despliegue para nuestro entorno de desarrollo Hadoop.
* Tenemos los archivos Dockerfile que se han utilizado para crear las imagenes
* Tenemos los archivos de configuració *.xml de los contenedores del cluster.
* Tenemos los script *.sh para arrancar los demonios de los contenedores.

En el archivo de despliegue (docker-compose.yml), se utilizan las imagenes que tengo almacenadas en mi cuenta de DockerHub. 
